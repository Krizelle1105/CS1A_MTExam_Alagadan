let studName = prompt("Enter your name:");
let subTitle = prompt("Enter the subject title:");
let classSched = prompt("Enter the class schedule (Time, Days):");
let classroom = prompt("Enter the classroom number:");
let classInstructor = prompt("Enter the class instructor's name:");

console.log('${studName} is currently enrolled in ${subTitle} with a class schedule of ${classSched} at room ${classroom}. The instructor for the subject is ${classInstructor}');